<!DOCTYPE html>
<html>
<head>
    <title>Posts</title>
</head>

<body>
    <h1>Posts</h1>
    <ul>
        {% for post in posts %}
            <li>
                <h2>{{post.title}}</h2>
                <p>{{post.body}}</p>
                <small>Posted on: {{post.date}}</small>
            </li>
        {% endfor %}
    </ul>
</body>
</html>